ng-repeat:(directive)

Used to iterate the elements from an collection.

Syntax:

ng-repeat = "x in collection"

Ex:

ng-repeat = "x in [10,20,30,40,50]"

{{x}}

Ex:
Access the data from controller

ng-init:

Used to initilize the static data.

we can declare static data in the form of a key and value pairs

each key & value is separated by using semi colan (;)